version: '3.8'
services:
  vulnerable-app:
    image: bkimminich/juice-shop:latest
    ports:
      - "3000:3000"
    networks:
      - lab-net  # <--- Define a named network

networks:
  lab-net:      # <--- Create the bridge network
    driver: bridge
