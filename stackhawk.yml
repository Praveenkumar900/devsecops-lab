# stackhawk.yml
app:
  applicationId: af84a5d9-0d41-4fa8-be44-79c41ae70ff8
  host: http://vulnerable-app:3000
  env: Development

hawk:
  spider:
    base: true
  # We removed the 'scanner' section because it was causing the error.
  # StackHawk will now run its default "Full Scan" policy.
